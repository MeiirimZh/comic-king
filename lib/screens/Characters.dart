import 'package:flutter/material.dart';

class Characters extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Персонажи', style: TextStyle(fontSize: 30))
    );
  }
}